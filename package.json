{
  "name": "omp-pet-workspace",
  "private": true,
  "version": "0.2.0",
  "packageManager": "bun@1.3.14",
  "workspaces": [
    "packages/*",
    "runtime"
  ],
  "scripts": {
    "build": "bun run build:core && bun run build:extension && bun run build:frontend",
    "build:core": "bun run --cwd packages/core build",
    "build:extension": "bun run --cwd packages/omp-extension build",
    "build:frontend": "bun run --cwd runtime build",
    "check": "bun run typecheck && bun test && bun run check:rust",
    "check:rust": "cargo check --manifest-path runtime/src-tauri/Cargo.toml",
    "dev": "bun run --cwd runtime tauri dev",
    "runtime:build": "bun run --cwd runtime tauri build",
    "package:windows": "bun run build:extension && bun run runtime:build && bun scripts/package-extension.ts",
    "verify:package": "bun scripts/verify-package.ts",
    "smoke:runtime": "bun scripts/smoke-runtime.ts",
    "test": "bun test",
    "typecheck": "bun run --cwd packages/core typecheck && bun run --cwd packages/omp-extension typecheck && bun run --cwd runtime typecheck"
  },
  "devDependencies": {
    "@types/bun": "^1.2.21",
    "scheduler": "0.27.0",
    "typescript": "^5.9.3"
  }
}
